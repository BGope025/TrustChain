-- TrustChain: Master Seed Script
-- Loads initial mock data into the database tables.

-- In a real setup, this might use \copy or custom scripts to load the JSON files
-- from the seed-data/ folder into Postgres.

SELECT 'Database seeded successfully with mock data.';
